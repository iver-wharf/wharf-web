---
exclude_paths:
  # The "projects" directory is autogenerated by the "generate-rest-client.ps1" script
  # No need to verify the code standards there as we don't strictly control that code
  - "projects/**"
